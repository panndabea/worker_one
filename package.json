{
  "name": "stripe-minimal-app",
  "version": "1.0.0",
  "description": "A super minimal Stripe payment app with static frontend and Cloudflare Worker backend",
  "main": "src/worker.js",
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "stripe": "^16.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.6.0"
  }
}
