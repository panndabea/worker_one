{
  "name": "worker_one",
  "version": "1.0.0",
  "description": "A simple Cloudflare Worker",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "vitest": "^2.1.9",
    "wrangler": "^4.75.0"
  }
}
